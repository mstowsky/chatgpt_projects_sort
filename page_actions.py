# ============================================================
# ДЕЙСТВИЯ С ЭЛЕМЕНТАМИ СТРАНИЦЫ
# ============================================================


class PageActions:

    def __init__(self, page):
        self.page = page

    # --------------------------------------------------------
    # Поиск элементов
    # --------------------------------------------------------

    def find_by_role(
        self,
        role,
        name=None
    ):
        """
        Находит элемент по ARIA role.
        """

        if name is not None:

            return self.page.get_by_role(
                role,
                name=name
            )

        return self.page.get_by_role(role)

    # --------------------------------------------------------
    # Нажатие
    # --------------------------------------------------------

    def click(self, locator):
        """
        Нажимает на переданный элемент.
        """

        locator.click()

    # --------------------------------------------------------
    # Получение текста
    # --------------------------------------------------------

    def get_text(self, locator):
        """
        Возвращает видимый текст элемента.
        """

        return locator.inner_text()

    # --------------------------------------------------------
    # Получение атрибута
    # --------------------------------------------------------

    def get_attribute(
        self,
        locator,
        attribute
    ):
        """
        Возвращает значение атрибута элемента.
        """

        return locator.get_attribute(attribute)

    # --------------------------------------------------------
    # Ввод текста
    # --------------------------------------------------------

    def fill(
        self,
        locator,
        text
    ):
        """
        Очищает поле и вводит текст.
        """

        locator.fill(text)

    # --------------------------------------------------------
    # Фокус
    # --------------------------------------------------------

    def focus(self, locator):
        """
        Устанавливает фокус на элемент.
        """

        locator.focus()

    # --------------------------------------------------------
    # Количество элементов
    # --------------------------------------------------------

    def count(self, locator):
        """
        Возвращает количество найденных элементов.
        """

        return locator.count()